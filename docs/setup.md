# C program setup

```

curl -s https://packagecloud.io/install/repositories/cs50/repo/script.deb.sh | sudo bash
sudo apt-get install libcs50
sudo apt autoremove

cc hello.c -o hello -lcs50 # to run code

```
